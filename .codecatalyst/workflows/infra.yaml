Name: terraform
SchemaVersion: "1.0"

# Optional - Set automatic triggers.
Triggers:
  - Type: Push
    FilesChanged:
      - .codecatalyst/.*
      - terraform/.*
      - cloudformation/.*

# Required - Define action configurations.
